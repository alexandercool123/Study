# Consolidated fact-check changelog

250 items checked across 11 lectures. 4 wrong / 6 missing / 13 incomplete. Read-only; no JSON edited.

## WRONG — factual errors (fix first)
- **L1 L1-1b-3-keymath** (wrong-vs-source): The drill attributes the 561,750 figure to the n(n-1)/2 formula, but 561,750 = 750x749 = n(n-1) (the directed key count), while n(n-1)/2 for 750 users is 280,875 — so the modelAnswer ties the wrong formula to the number and is internally inconsistent (it gives 15 = n(n-1)/2 for 6 users but n(n-1) for 750).
  - fix: Clarify per the slide: number of distinct pairwise KEYS = n(n-1)/2 (6 users -> 15; 750 users -> 280,875). The slide's 561,750 = 750x749 = n(n-1) counts directed key-ends / 'keys in the system', NOT key pairs. State both explicitly so the student does not present 561,750 as the n(n-1)/2 result. Same fix applies to L1-EX-2 which repeats '750 employees need 561,750 keys' under the key-pair framing.
- **L1 L1-EX-2-keymath** (wrong-vs-source): Exercise answerSketch states 'each user pair needs an individual key, n(n-1)/2 total, e.g. 750 employees need 561,750 keys' — but 561,750 is n(n-1), not n(n-1)/2 (=280,875), so the formula and number contradict each other.
  - fix: Use the slide's own split: n(n-1)/2 key pairs (280,875 for 750), and n(n-1)=561,750 as the total 'keys in the system'. Do not equate the n(n-1)/2 formula with 561,750.
- **L3 L3-3b-3-tgt-target** (wrong-vs-reality): The drill states the TGT issued by the AS contains 'the target server details', implying the TGT is bound to a specific service; in real Kerberos the TGT is generic and the target service's identity appears only in the service ticket the TGS later issues.
  - fix: Keep the source wording but add a one-line clarification that the TGT is presented to the TGS to obtain a *service-specific* ticket (PAGE 43: 'the TGS generates a service-specific ticket'), so it is the service ticket — not the TGT — that is bound to a particular target server. Cite RFC 4120 / standard Kerberos AS-exchange vs TGS-exchange distinction.
- **L6 L6-6a-3-cite** (wrong-vs-source): The drill's password/identity-theft/phishing claim is cited to saml-101 PAGE 06, but that exact content ('user password never crosses the corporate firewall', 'eliminates opportunities for identity theft', 'phishing') is on the LECTURE slide PAGE 12 and white-paper PAGE 09, not PAGE 06 (PAGE 06 only carries the Users/IT/Business 'love it' material).
  - fix: Minor: the two claims are correctly drawn from the lecture but span two pages; split the citation (PAGE 06 for the pros, Lecture PAGE 12 / white-paper PAGE 09 for the firewall/theft point). Substance is accurate; non-blocking.

## MISSING — coverage gaps (a sub-part no drill answers)
- **L8 L8-7c-anchor-note** (missing): Not a content error: the six EUDI-Wallet drills anchor examQ 7 subPart c, which is NOT in _intent_8.txt (that file lists only Q2 a/b/c). Verified against _map.json: lecture 8 supports [2,7] and examQ7c is exactly the VC/SSI roles (issuer-holder-verifier) sub-part, so the mapping is valid and these drills are NOT orphans. _intent_8.txt is simply incomplete; no JSON change needed.
  - fix: No drill change. Optionally note in _intent_8.txt that lecture 8 also cross-supports examQ7c (VCs/SSI roles) so the mapping is documented; the drills themselves are correctly anchored.
- **L9 L9-5d-MAP** (missing): Two drills (L9-5d-1, L9-5d-2) anchor to exam sub-part '5d', but exam Q5 has only sub-parts a/b/c — there is no 5d, so these drills map to a non-existent exam sub-part.
  - fix: Re-anchor L9-5d-1 and L9-5d-2 to a real sub-part or drop the sub-part claim. Their substance (incremental ROI, centralized governance body) most plausibly supports 5c (RBAC/ABAC governance challenges) at best, but it is implementation-strategy padding with no home in Q5's a/b/c; set anchorsSubPart to a valid letter (e.g. 'c') only if reframed around access-governance, otherwise treat as out-of-scope for the exam.
- **L9 L9-5e-MAP** (missing): Drill L9-5e-1 anchors to exam sub-part '5e', which does not exist (Q5 stops at c).
  - fix: Drop the '5e' anchor. If retained for cross-support, anchor to Q6 (SSO) as a crossRef-style support drill, or re-anchor to 5a ('how can authorization be implemented') only if reframed around fine-grained/scoped access rather than SSO.
- **L9 L9-Q2-MAP** (missing): Three drills (L9-2a-1, L9-2b-1, L9-2b-2) anchor to Q2 a/b, but (i) Q2's crossRefs in _map.json are [6,8] — lecture 9 is NOT a declared cross-ref for Q2, and (ii) Q2's actual sub-parts a/b are about 'what is a digital identity' and 'identification vs identity proofing vs authentication', which these JML/HRMS/merger-split drills do not address.
  - fix: Either remove the Q2 anchor (this guest lecture's intent file only commits it to Q5) or, if kept, re-anchor to Q2's lifecycle theme via lecture 2's own framing — but Q2a/b text does not cover JML, so the cleanest fix is to label these as Q5-supporting (identity provisioning under access governance) rather than Q2.
- **L10 L10-7c-missing-VP** (missing): No drill covers the Verifiable Presentation — how the holder actually presents a VC to the verifier (signed with a verifier nonce for replay protection), which is the holder-side mechanism the owned sub-part 7c ('who are the key roles ... and what do they do') asks about.
  - fix: Add one drill: holder packages the VC into a Verifiable Presentation, signs it with a nonce supplied by the verifier so it cannot be replayed; this is the holder's active role and the last step of the trust triangle (issuer issues VC -> holder holds in wallet -> holder presents VP -> verifier checks issuer signature). Grounded in IAM - JWT.txt PAGE 43.
- **L11 L11-9b-validation** (missing): No drill states HOW the client validates the ID token, even though Q9b asks for the tokens' working principles. The flow drill (L11-9b-3) and token-structure drill (L11-9b-5) say the client 'validates the tokens' but never define validation (verify JWS signature against the OP's RSA/EC key; check iss, aud=client_id, exp, and nonce for replay).
  - fix: Add a beat to L11-9b-3 or L11-9b-5: the client validates the ID token by checking the JWS signature with the OP's public RSA/EC key and verifying iss, aud (its own client_id), exp, and the nonce (replay protection), per PAGE 15-16.

## INCOMPLETE — set named but not fully explained
- **L2 L2-2a-5** (incomplete): Drill names all 7 Laws of Identity but explains only the first two (User Control/Consent and Minimal Disclosure); if the examiner asks about law 3-7 the student has nothing, even though sub-part 2a asks for identity 'principles'.
  - fix: Add a one-line gloss for laws 3-7 grounded in TheLawsOfIdentity.txt PAGES 07-11: (3) Justifiable Parties = only parties with a necessary/justifiable role receive identifying info, each must give a use-policy; (4) Directed Identity = omni-directional identifiers for public entities, unidirectional (per-site) identifiers for private ones to prevent correlation; (5) Pluralism of Operators and Technologies = a metasystem inter-working multiple IdPs and identity technologies, no single monolith; (6) Human Integration = make the human a reliable component via unambiguous 'ceremony' resistant to phishing; (7) Consistent Experience Across Contexts = a simple, consistent UX while keeping contexts separated.
- **L2 L2-EX-6** (incomplete): Exercise answerSketch for the Laws-of-Identity / SSI / GDPR comparison only NAMES Cameron's 7 laws and Allen's 10 principles without a speakable gloss, so it does not deliver the set at exam depth for sub-part 2a.
  - fix: Attach the same one-line-per-member glosses as the L2-2a-5 fix for the 7 laws, and confirm the 10 SSI principles each have a one-line meaning (Existence, Control, Access, Transparency, Persistence, Portability, Interoperability, Consent, Minimalization, Protection) from Lecture-2.txt PAGE 42 / Alacrity lines 134-143.
- **L3 L3-3b-as-tgs-flow-set** (incomplete): Sub-part (b) is a SET question (AS exchange -> TGT, TGS exchange -> service ticket, client/server exchange). The drills explain the AS->TGT and TGT->TGS steps and the conceptual KDC ticket model, but no drill cleanly walks the third leg (client presenting the service ticket to the actual server) as a labelled stage of the three-exchange flow.
  - fix: Add one beat (grounded in PAGE 49-50) making the three Kerberos exchanges explicit: (1) AS exchange returns TGT + session key; (2) TGS exchange: present TGT, get service ticket {A,Kab,T}mKbs for Bob; (3) client presents that service ticket to Bob, timestamp checked against replay, then Alice and Bob communicate.
- **L4 L4-4c-4-note** (wrong-vs-reality): The phishing-resistance drill says the authenticator 'will not produce a valid signature for that origin', which slightly mislocates the mechanism: in real WebAuthn the authenticator does sign (over clientDataHash, which embeds the browser-supplied origin/RP ID), and it is the RELYING PARTY that rejects the mismatched origin/RP ID; the browser also refuses to invoke a credential whose RP ID does not match the page's origin.
  - fix: Rephrase to: the browser binds the credential to the registered RP ID/origin so it won't release/sign for a non-matching origin, and the relying party verifies the origin/RP ID it receives — on a fake site this check fails, so no usable authentication results. (Mechanism still yields the same exam conclusion: phishing fails.) Source only poses the scenario on PAGE 27, so this refinement comes from the WebAuthn standard.
- **L5 L5-5b3-context-handler** (incomplete): The XACML 3.0 architecture drill (5b-3) names only PEP/PDP/PAP/PIP, but the lecture's central data-flow figure also makes the context handler a first-class component, and an examiner asking to 'explain the policy architecture' (Q5b) would expect it to appear alongside the four points.
  - fix: Add a fifth bullet to 5b-3 (or cross-note from 5b-5): the context handler converts the native request into XACML canonical form, gathers attributes by querying the PIP, and mediates between PEP and PDP. 5b-5 already covers it, so this is a depth/cross-link note, not a contradiction.
- **L5 L5-5b4-combining-scope** (incomplete): 5b-4 presents deny-overrides / permit-overrides / first-applicable / only-one-applicable but does not state that 'only-one-applicable' is defined ONLY for policy/policy-set combining (not rules), which is the exact distinction an examiner probing combining algorithms tends to test.
  - fix: Note in 5b-4 that only-one-applicable applies to policies/policy-sets (by their targets), yielding NotApplicable if none and Indeterminate if more than one apply, while deny/permit-overrides and first-applicable exist at both rule- and policy-combining levels. Grounded in PAGE 43.
- **L6 L6-6a-3-firewall-caveat** (wrong-vs-source): The drill states flatly 'With SAML the user password never crosses the firewall' but the lecture source explicitly qualifies this to IdP-initiated SAML SSO; an examiner who knows the slide could push on the generalisation.
  - fix: Add the source's parenthetical caveat '(in the case of IdP-initiated SAML SSO)' so the claim matches the slide's scope. Low severity.
- **L7 L7-8b3-grantcount** (wrong-vs-reality): Q8b says 'grants' and the OAuth canon is usually taught as 'four grant types'; the drill frames the set as Authorization Code, Auth-Code-with-PKCE, Client Credentials, Device Code plus two legacy, which is faithful to the slide but does not flag that PKCE is a variant of the Authorization Code grant (not a 5th independent grant) nor that the classic RFC 6749 four are Authorization Code / Implicit / ROPC / Client Credentials.
  - fix: In L7-8b-3 add a one-line clarification that PKCE is an add-on to the Authorization Code grant rather than a separate grant, and that the four canonical RFC 6749 grants are Authorization Code, Implicit, ROPC and Client Credentials (Device Code being a later RFC 8628 addition) — so the student can answer either framing.
- **L7 L7-8b6-endpointcite** (wrong-vs-source): L7-8b-6 ('two OAuth endpoints') cites only PAGE 22, but PAGE 22 only shows the authorization request with scopes; the authorization-endpoint vs token-endpoint distinction and the front-channel/back-channel split it relies on are actually established on PAGE 17 (and PAGE 20-21/26). The content is correct but the single citation under-supports it.
  - fix: Broaden L7-8b-6's citation to 'PAGE 17 (+ 20-22, 26)' so the front-channel authorization endpoint / back-channel token endpoint claim is grounded where the slide actually draws it.
- **L7 L7-8b5-refresh-neversenttoRS** (wrong-vs-source): L7-8b-5 asserts as a key beat 'refresh tokens are never sent to the RS'. This is true in reality (RFC 6749 §1.5: refresh tokens are used only at the AS) but is NOT stated on cited PAGE 23, so it should be marked as a reality-grounded claim, not a source quote.
  - fix: Keep the beat but attribute the 'never sent to the RS' point to RFC 6749 §1.5 rather than implying PAGE 23 states it; the slide supports only 'traded in at the AS'.
- **L8 L8-2a-2-incomplete** (incomplete): The drill asks which means combinations reach substantial vs high, but its substantial set omits 'Password + MitID chip', which the slide lists under Level Substantial (in addition to Level High).
  - fix: Add 'Password + MitID chip' to the substantial combination list (per PAGE 09) so the substantial set is complete; note the slide lists this combination under both substantial and high.
- **L10 L10-7c-4-nuance-revocation** (incomplete): Drill 7c-4 asserts the verifier 'does not have to contact the issuer at verification time' as an unqualified rule; the lecture's own VC example shows a credentialStatus/revocation list, so for revocation the verifier may still consult an issuer-published status endpoint.
  - fix: Qualify 7c-4: signature/authenticity verification is offline against the issuer's public key, but revocation/validity may require checking the issuer-published credentialStatus list (W3C status list shown on PAGE 36). Keeps the core 'trust flows from issuer signature' point while not overstating full offline verification.
- **L11 L11-9c-6-responsemode** (incomplete): Drill L11-9c-6 (and exercise L11-EX-23, 'problem with query and id_token') answers only vaguely ('response type/mode must be set correctly'). The actual point — an id_token must NOT be returned via the query response_mode (it must go in the fragment / form_post) because a token in the query string leaks via browser history, Referer and server logs — is not stated, so a student cannot explain WHY there is a problem.
  - fix: Make L11-9c-6 state: requesting a response_type containing id_token with response_mode=query is disallowed because an id_token in the query string would leak (history/Referer/logs); OIDC requires fragment or form_post. Name the standard (OIDC Core 1.0) as the basis.
